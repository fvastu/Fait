'use client'

import {
  Box,
  Flex
} from '@chakra-ui/react'

export default function Hero() {
  return (
    <Box>
      <Flex
        minH={'60px'}
        py={{ base: 2 }}
        px={{ base: 4 }}
        align={'center'}>
      </Flex>
    </Box>
  )
}